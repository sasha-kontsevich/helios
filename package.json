{
  "name": "helios-monorepo",
  "private": true,
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "scripts": {
    "start": "npm run start --workspace examples/astris",
    "build": "npm run build --workspace packages/helios-core",
    "test": "npm run test --workspace packages/helios-core"
  },
  "devDependencies": {
    "pnpm": "10.0.0"
  }
}
