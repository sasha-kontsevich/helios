{
  "name": "helios-monorepo",
  "private": true,
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "scripts": {
    "start": "npm run start --workspace examples/astris",
    "build": "pnpm -r run build",
    "typecheck": "pnpm -r run typecheck",
    "test": "pnpm -r run test || true"
  },
  "devDependencies": {
    "pnpm": "10.0.0"
  }
}
