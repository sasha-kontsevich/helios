import { defineComponent } from "@merlinn/helios-core";

/** Tag: entity receives game-viewport fly/orbit systems driven by {@link VIEWPORT_INPUT_CAPABILITY}. */
export const ViewportCameraControl = defineComponent({});
